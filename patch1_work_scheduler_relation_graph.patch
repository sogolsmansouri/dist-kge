diff --git a/kge/distributed/work_scheduler.py b/kge/distributed/work_scheduler.py
--- a/kge/distributed/work_scheduler.py
+++ b/kge/distributed/work_scheduler.py
@@ -1,3 +1,4 @@
+import numpy as np
 import torch
@@
 def _register_partition_relation_gradient(
         self, partition_id, rel_ids, rel_sums, rel_counts
     ):
@@
-        try:
-            self._update_gradient_graph(
-                partition_id, rel_ids_list, rel_sums_list, rel_counts_list
-            )
-        except Exception as exc:
-            self.config.log(
-                "Failed to update gradient graph "
-                f"(partition_id={partition_id}): {exc}"
-            )
+        # Normalize complex/tuple/window ids to base int partition ids before touching the graph.
+        base_pids = self._base_partition_ids(partition_id)
+        if not base_pids:
+            return
+        try:
+            for pid in base_pids:
+                self._update_gradient_graph(pid, rel_ids_list, rel_sums_list, rel_counts_list)
+        except Exception as exc:
+            self.config.log(
+                "Failed to update gradient graph "
+                f"(partition_id={partition_id}, base_pids={base_pids}): {exc}"
+            )
